{
  "name": "othello",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "fetch-fixtures": "tsx ops/fetch-fixtures.ts",
    "verify-issuer-bindings": "tsx ops/verify-issuer-bindings.ts",
    "test": "mocha --import=tsx --timeout 120000 \"tests/**/*.spec.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "0.32.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.0",
    "mocha": "^12.0.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
